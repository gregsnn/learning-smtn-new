const adi = function(a = 15, b = 10) {
    return a + b
}

const sub = function(a = 15, b = 10) {
    return a - b
}

const multi = function(a = 15, b = 10) {
    return a * b
}

const div = function(a = 15, b = 10) {
    return a / b
}

console.log("Soma: " + adi() + ", Subtração: " + sub() + ", Multiplicação: " + multi() + ", Divisão: " + div())

// (y)